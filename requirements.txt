# ─── Core (demo + production) ────────────────────────────────
# Versions pinned to what's available for Python 3.14+
# pydantic-core requires Rust toolchain to build from source on Python 3.14;
# use the versions that ship pre-built wheels for your Python version.
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0
pydantic-settings>=2.2.0
sqlalchemy>=2.0.30
aiosqlite>=0.20.0
python-dotenv>=1.0.0
PyYAML>=6.0.1
httpx>=0.27.0
python-multipart>=0.0.9
anyio>=4.3.0

# ─── Testing ─────────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0

# ─── Production-only (not needed for demo/tests) ─────────────
# Uncomment when deploying to production:
# asyncpg>=0.29.0       # PostgreSQL async driver
# aiokafka>=0.10.0      # Kafka audit/alert integration
# paramiko>=3.4.0       # SFTP file transfer
# boto3>=1.34.0         # S3-compatible storage
